/**
 * Central XP economy — rewards demonstrated mastery, not passive reading.
 */

export const XP_SECTION_QUIZ = 50;
export const XP_QUIZ_PERFECT_BONUS = 25;
export const XP_ISLAND_COMPLETION = 200;
export const XP_STREAK_DAILY_BONUS = 0;
export const XP_TEN_Q_COMPLETION = 300;
export const XP_EARNINGS_CALL_COMPLETION = 150;

/** One-time bonus when quiz streak first reaches 3 consecutive days (lifetime per company). */
export const XP_QUIZ_STREAK_3 = 50;
/** One-time bonus at 7-day quiz streak. */
export const XP_QUIZ_STREAK_7 = 150;
/** One-time bonus at 30-day quiz streak. */
export const XP_QUIZ_STREAK_30 = 500;
