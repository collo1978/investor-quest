import { ProfileStubClient } from "../ProfileStubClient";

export default function SectorStrengthPage() {
  return (
    <ProfileStubClient
      title="Sector strength"
      description="Sector-level conviction and exploration — opening soon from the profile hub."
    />
  );
}
