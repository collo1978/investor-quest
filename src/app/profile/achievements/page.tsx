import { ProfileStubClient } from "../ProfileStubClient";

export default function ProfileAchievementsPage() {
  return (
    <ProfileStubClient
      title="Achievements"
      description="Full badge case and mastery timeline — opening soon from the profile hub."
    />
  );
}
