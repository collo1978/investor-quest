import { SchoolsLogoIntroScreen } from "@/components/schools/SchoolsLogoIntroScreen";

/** Logo float-in opener — auto-advances to mission brief cards. */
export default function SchoolsLogoIntroPage() {
  return <SchoolsLogoIntroScreen />;
}
