import SchoolsDuolingoBoardMapPageClient from "./pageClient";

/** Fullscreen preview — flat 2D Duolingo-style board map (does not replace `/schools/map`). */
export default function SchoolsDuolingoBoardMapPreviewPage() {
  return <SchoolsDuolingoBoardMapPageClient />;
}
