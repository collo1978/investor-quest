"use client";

/**
 * Schools variant — onboarding.
 * Starts as a copy of Bank/Broker onboarding, but lives on its own route so
 * future school-specific changes won't touch `/onboarding` or production `/demo`.
 */
export { default } from "@/app/onboarding/page";

