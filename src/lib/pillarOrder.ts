/**
 * Legacy compatibility shim.
 *
 * Pillar ordering now lives in the data layer (`@/data/pillars`).
 */

export { PILLAR_ORDER, nextPillarId } from "@/data/pillars";
export type { PillarId } from "@/data/pillars";
