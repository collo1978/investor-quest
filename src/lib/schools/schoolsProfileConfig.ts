/** Schools profile artwork (`public/logos/school-profile.png`). */
export const SCHOOLS_PROFILE_IMAGE_SRC = "/logos/school-profile.png";

export const SCHOOLS_PROFILE_IMAGE_NATURAL = {
  width: 1672,
  height: 941
} as const;

/** Armor guide CTA — investor armor card (1672×941). */
export const SCHOOLS_PROFILE_ARMOR_GUIDE_HIT = {
  left: (42 / 1672) * 100,
  top: (252 / 941) * 100,
  width: (126 / 1672) * 100,
  height: (38 / 941) * 100
} as const;

/** Company mastery CTA — top-right of Company Mastery card (1672×941). */
export const SCHOOLS_PROFILE_COMPANY_MASTERY_CTA_HIT = {
  left: (500 / 1672) * 100,
  top: (504 / 941) * 100,
  width: (108 / 1672) * 100,
  height: (36 / 941) * 100
} as const;

/** XP Ladder CTA — top-right beside XP earned (1672×941). */
export const SCHOOLS_PROFILE_XP_LADDER_HIT = {
  left: (1260 / 1672) * 100,
  top: (92 / 941) * 100,
  width: (152 / 1672) * 100,
  height: (44 / 941) * 100
} as const;
